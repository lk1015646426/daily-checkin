name: Daily Checkin

on:
  schedule:
    # 每天 20:00 UTC = 次日北京时间 04:00
    - cron: "0 20 * * *"
  workflow_dispatch:
    inputs:
      account_filter:
        description: "可选，格式为站点:账号名，例如 trae:刘浩17721"
        required: false
        default: ""

concurrency:
  group: daily-checkin
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  checkin:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 每次运行使用唯一键保存最新认证；只从 v2 前缀恢复，避免复用旧版缓存。
      - name: Restore token cache
        uses: actions/cache@v4
        with:
          path: store
          key: signin-token-cache-v2-${{ github.run_id }}
          restore-keys: |
            signin-token-cache-v2-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install chromium

      - name: Run checkin
        env:
          CHECKIN_ACCOUNT_FILTER: ${{ github.event.inputs.account_filter || '' }}
          ACY7_USER: ${{ secrets.ACY7_USER }}
          ACY7_PASS: ${{ secrets.ACY7_PASS }}
          WB1_TOKEN: ${{ secrets.WB1_TOKEN }}
          WB2_TOKEN: ${{ secrets.WB2_TOKEN }}
          A_1780293_TOKEN: ${{ secrets.A_1780293_TOKEN }}
          A_1780293_DEVICE_ID: ${{ secrets.A_1780293_DEVICE_ID }}
          A_1780293_DEVICE_BRAND: ${{ secrets.A_1780293_DEVICE_BRAND }}
          A_1780293_DEVICE_TYPE: ${{ secrets.A_1780293_DEVICE_TYPE }}
          A_1920293_TOKEN: ${{ secrets.A_1920293_TOKEN }}
          A_1920293_DEVICE_ID: ${{ secrets.A_1920293_DEVICE_ID }}
          A_1920293_DEVICE_BRAND: ${{ secrets.A_1920293_DEVICE_BRAND }}
          A_1920293_DEVICE_TYPE: ${{ secrets.A_1920293_DEVICE_TYPE }}
          A_17721_TOKEN: ${{ secrets.A_17721_TOKEN }}
          A_17721_DEVICE_ID: ${{ secrets.A_17721_DEVICE_ID }}
          A_17721_DEVICE_BRAND: ${{ secrets.A_17721_DEVICE_BRAND }}
          A_17721_DEVICE_TYPE: ${{ secrets.A_17721_DEVICE_TYPE }}
          A_1807095_TOKEN: ${{ secrets.A_1807095_TOKEN }}
          A_1807095_DEVICE_ID: ${{ secrets.A_1807095_DEVICE_ID }}
          A_1807095_DEVICE_BRAND: ${{ secrets.A_1807095_DEVICE_BRAND }}
          A_1807095_DEVICE_TYPE: ${{ secrets.A_1807095_DEVICE_TYPE }}
          A_4028161677_TOKEN: ${{ secrets.A_4028161677_TOKEN }}
          A_4028161677_DEVICE_ID: ${{ secrets.A_4028161677_DEVICE_ID }}
          A_4028161677_DEVICE_BRAND: ${{ secrets.A_4028161677_DEVICE_BRAND }}
          A_4028161677_DEVICE_TYPE: ${{ secrets.A_4028161677_DEVICE_TYPE }}
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }}
        run: python main.py
